package com.arsoban.util

import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.platform.Font

data class Fonts(
    val firstFontFamily: FontFamily = FontFamily(
        Font(
            "fonts/bahnschrift.ttf"
        )
    )
)
